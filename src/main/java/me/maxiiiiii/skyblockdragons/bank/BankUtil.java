package me.maxiiiiii.skyblockdragons.bank;

import org.bukkit.entity.Player;

public class BankUtil {
    public static void add(Player player, double amount) {
//        setVariable(player.getUniqueId(), "BankPersonal", (bankPersonal.getOrDefault(player.getUniqueId(), 0d) + amount) + "");
//        bankPersonal.put(player.getUniqueId(), bankPersonal.getOrDefault(player.getUniqueId(), 0d) + amount);
    }

    public static void set(Player player, double amount) {
//        setVariable(player.getUniqueId(), "BankPersonal", amount + "");
//        bankPersonal.put(player.getUniqueId(), amount);
    }
}
